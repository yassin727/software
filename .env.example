# Server Configuration
PORT=4000
NODE_ENV=development

# Database Configuration (Local Development Only)
MONGODB_LOCAL=mongodb://localhost:27017/hmts

# Production Database (Railway)
# MONGO_URL=mongodb://...

# Security
JWT_SECRET=super_secret_jwt_key

# Application URL (for email links)
APP_URL=http://localhost:4000

# ============================================================
# Email Configuration (SMTP)
# ============================================================
# Required for sending maid approval/rejection emails
# 
# For Gmail:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   SMTP_USER=your-email@gmail.com
#   SMTP_PASS=your-app-password (generate at https://myaccount.google.com/apppasswords)
#
# For SendGrid:
#   SMTP_HOST=smtp.sendgrid.net
#   SMTP_PORT=587
#   SMTP_USER=apikey
#   SMTP_PASS=your-sendgrid-api-key
#
# For Mailgun:
#   SMTP_HOST=smtp.mailgun.org
#   SMTP_PORT=587
#   SMTP_USER=postmaster@your-domain.mailgun.org
#   SMTP_PASS=your-mailgun-password

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
FROM_EMAIL=noreply@maidtrack.com
FROM_NAME=MaidTrack

# Optional: File Upload Configuration
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=./public/uploads